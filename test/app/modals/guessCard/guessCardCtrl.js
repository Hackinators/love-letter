// INITILIZE CONTROLLER
// ============================================================
angular.module("app")
	.controller("guessCardCtrl", function($scope, close, cards) {

		// VARIABLES
		// ============================================================
		$scope.close = close;
		$scope.cards = cards;

		// FUNCTIONS
		// ============================================================


	});
